<ContentPage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="ErastourApp.Pages.LoginPage">

    <Grid>
        <VerticalStackLayout
            VerticalOptions="Center"
            HorizontalOptions="Center"
            Spacing="20"
            WidthRequest="280">

            <Label
                Text="Connexion"
                FontSize="28"
                FontAttributes="Bold"
                HorizontalTextAlignment="Center" />

            <Entry
                Placeholder="Email"
                Text="{Binding Email}" />

            <Entry
                Placeholder="Mot de passe"
                ClearButtonVisibility="WhileEditing"
                IsPassword="True"
                Text="{Binding Password}" />

            <Button
                Text="Se connecter"
                Command="{Binding LoginCommand}" />

            <Label
                Text="Note : Les pages trajet et vue d'ensemble n'existent pas, donc cliquer sur ces pages dans la barre de navigation fera planter l'application."
                HorizontalTextAlignment="Center" />

        </VerticalStackLayout>
    </Grid>
</ContentPage>